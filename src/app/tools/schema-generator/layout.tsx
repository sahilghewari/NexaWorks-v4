export const metadata = {
  alternates: { canonical: "https://nexaworks.tech/tools/schema-generator" }
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
