export const metadata = {
  alternates: { canonical: "https://nexaworks.tech/tools/context-score" }
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
